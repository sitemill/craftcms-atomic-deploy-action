name: 'Craft CMS - Atomic Deploy Action'
description: 'An all-in-one action to Rsync + Atomically deploy your Craft project.'
inputs:
  host:
    description: 'Server IP'
    required: true
  port:
    description: 'Server Port'
    required: false
    default: 22
  user:
    description: 'Server User'
    required: true
  ssh_key:
    description: 'Private SSH key'
    required: true
  remote_path:
    description: 'Absolute remote path'
    required: true
  post_deploy:
    description: 'The commands to run after deploy'
    required: false
outputs:
  webroot:
    description: 'The new webroot'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.host }}
    - ${{ inputs.port }}
    - ${{ inputs.user }}
    - ${{ inputs.ssh_key }}
    - ${{ inputs.remote_path }}
    - ${{ inputs.post_deploy }}
branding:
  icon: 'upload-cloud'
  color: 'red'